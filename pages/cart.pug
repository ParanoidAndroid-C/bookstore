html
head
  link(rel='stylesheet' href='/stylesheets/cart.css')
  meta(charset="utf-8")
header
  ul#menu
    li
      a(href='../') Main Page
main
    h3 Items:
    each book in books
        -bid = book.book_id
        .column
        .card(onclick='showBook("'+bid+ '")')
            p #{book.title}
            p #{book.price}
    br

footer
script(src='/public/author.js')
